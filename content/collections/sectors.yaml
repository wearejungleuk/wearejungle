title: Sectors
route: '/sectors/{slug}'
template: sectors/show
layout: layout
sort_by: order
structure:
  root: false
  max_depth: 1
inject:
  breadcrumb_parent: Sectors
preview_targets:
  -
    label: Entry
    url: '{permalink}'
    refresh: true
